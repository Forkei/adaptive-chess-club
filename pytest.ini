[pytest]
testpaths = tests
python_files = test_*.py
markers =
    live: opt-in tests that call the real LLM (requires GEMINI_API_KEY and RUN_LIVE_LLM_TESTS=1)
filterwarnings =
    ignore::DeprecationWarning
